{
  "name": "vulfram-core",
  "version": "0.0.1",
  "main": "index.node",
  "types": "index.d.ts",
  "scripts": {
    "build:ffi": "cargo build --release --features ffi --no-default-features",
    "build:napi": "napi build --release -o binding/napi --no-dts",
    "artifacts": "napi artifacts",
    "prepublishOnly": "napi prepublish -t npm"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.0"
  },
  "napi": {
    "binaryName": "vulfram-core"
  }
}
