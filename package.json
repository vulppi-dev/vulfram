{
  "name": "vulfram-core",
  "version": "0.0.1",
  "main": "index.node",
  "types": "index.d.ts",
  "scripts": {
    "build:ffi": "cargo build --release --features ffi --no-default-features",
    "build:napi": "napi build --release -o binding/src/napi",
    "artifacts": "napi artifacts",
    "prepublishOnly": "napi prepublish -t npm"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.5.0",
    "@types/bun": "^1.3.3"
  },
  "napi": {
    "binaryName": "vulfram-core"
  }
}
